export const initialState = () => ({
  isConvidAllLoading: false,
  covidAll: null,

  isHistoricalAllLoading: false,
  historicalAll: null,
});

// state
export default {
  ...initialState(),
};
